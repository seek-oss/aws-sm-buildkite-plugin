name: aws-sm
description: Read secrets from AWS Secrets Manager
author: https://github.com/seek-oss
requirements:
  - docker
  - jq
configuration:
  properties:
    env:
      type: [object, array]
    file:
      type: array
  required: []
